# 验收记录

最后更新：2026-09-17（Asia/Shanghai）

## 当前已通过

| 检查 | 命令或方式 | 结果 |
|---|---|---|
| TypeScript | `pnpm typecheck` | 通过 |
| ESLint | `pnpm lint` | 通过 |
| 单元与组件测试 | `pnpm test` | 9 个文件、51/51 通过 |
| 研究流程 E2E | Playwright `e2e/research.spec.ts` | 4/4 通过 |
| 生产构建 | `pnpm build` | 通过 |
| Railway 部署 | 公开地址访问 | 构建、启动与页面加载通过 |
| 生产健康检查 | `/api/health` | HTTP 200；`ok: true`、SQLite、demo mode |
| DeepSeek 结构化筛选 | 真实 API 烟雾测试 | 严格 JSON Schema 与 `output_text` 解析通过 |
| PDF 简历上传 | 生产接口回归 | 上传、提取与清理通过 |
| 项目置顶与删除 | 浏览器与隔离接口流程 | 置顶、取消置顶、排序、删除与空列表通过 |

公开部署：<https://baoyan-prep-production.up.railway.app>

## 覆盖的关键边界

- URL 安全：危险协议、环回、私网、链路本地和云元数据地址拒绝。
- 来源策略：规范化 URL、内容指纹、规则评分、时效、学校匹配与模型审核合并。
- 结构化响应：只读取 DeepSeek Responses 的 `output_text`，拒绝把 `reasoning_text` 当 JSON。
- 生成边界：无证据题目必须降级为通用补充或 AI 生成。
- 研究进度：启动前先选择档位，服务端以 NDJSON 返回阶段、进度、完成或失败事件。
- 题单生成：候选资料上限、NDJSON 阶段进度、心跳保活、连接提前关闭和网络失败提示。
- ZIP：项目资料批量导出、链接清单和安全文件名。
- 输入验证：项目、目标、来源、附件、简历与生成请求边界。
- 项目隔离：不同学校的目标、资料、研究任务和题单版本分开存储。

## 自动化测试说明

当前通过的 Playwright 用例覆盖资料收集弹窗的显式启动、档位选择、流式进度、失败恢复和完成刷新。仓库中如仍保留旧版 `core-flow` 用例，其页面文案与选择器属于早期界面，不计入当前通过结果；后续会按三个独立关卡补齐完整浏览器回归。

## 已完成人工验收

- ChatGPT 式项目侧栏、项目切换与三点菜单。
- 项目置顶、取消置顶和可靠删除。
- 资料库来源列表、全部删除、批量导出和错误恢复提示。
- 快速、标准、深度三档研究的选择与显式开始交互。
- DeepSeek 结构化资料复核的真实返回格式。
- Railway 公开部署、冷启动后的页面访问和 SQLite 健康检查。

## 尚未完成

- 使用有效 Tavily Key 对多个真实院校执行完整联网搜索质量评测。
- Railway 持久卷备份与灾难恢复演练。
- 低清扫描件和复杂图片 OCR 的系统化准确率测试。
- 大规模资料集下的延迟、成本和题目可追溯率评测。
- 正式用户访谈、任务完成率和训练价值验证。

这些限制会在 README 中如实披露，不把模型生成、演示数据或未运行的测试表述为真实结果。
