# 验收记录

最后更新：2026-09-17（Asia/Shanghai）

## 当前已通过

| 检查 | 命令或方式 | 结果 |
|---|---|---|
| TypeScript | `pnpm typecheck` | 通过 |
| ESLint | `pnpm lint` | 通过 |
| 单元测试 | `pnpm test` | 5 个文件、30/30 通过 |
| 生产构建 | `pnpm build` | 通过；23 个路由完成生成/编译 |
| 生产健康检查 | `/api/health` | `ok: true`、SQLite、real mode |
| DeepSeek 结构化筛选 | 真实响应烟雾测试 | 严格 JSON Schema 与 `output_text` 解析通过 |
| 项目置顶 | 当前浏览器实际操作 | 置顶、取消置顶和排序刷新通过 |
| 项目删除 | 隔离临时工作区 API 流程 | 创建—删除—空列表通过；现有项目未被修改 |

## 单元测试覆盖

- URL 安全：危险协议、环回、私网、链路本地和云元数据地址拒绝。
- 来源策略：规范化 URL、规则评分、时效、学校匹配与模型审核合并。
- 结构化响应：只读取 DeepSeek Responses 的 `output_text`，拒绝把 `reasoning_text` 当 JSON。
- 生成边界：无证据题目必须降级为通用补充或 AI 生成。
- ZIP：项目资料批量导出、链接清单和安全文件名。
- 输入验证：目标、来源、附件和训练请求边界。

## 当前 E2E 状态

`e2e/core-flow.spec.ts` 是大改版前留下的脚本，仍查找“调查档案”“专业面试”等旧版元素。2026-09-17 重新运行时 3 项均因旧选择器找不到当前界面而失败。当前不能声称端到端自动化已通过；需要按项目侧栏、资料库和三关页面重写这些脚本。

## 已完成的人工验收

- 浏览器中的项目切换与三点菜单。
- 项目置顶/取消置顶。
- 项目删除接口在隔离会话中的完整流程。
- 资料库清空、来源列表和错误恢复提示的基本交互。
- DeepSeek 结构化资料复核的真实返回格式。

## 尚未验证

- 公网部署、HTTPS、冷启动和持久卷恢复。
- 多种真实院校网站的大规模抓取质量与反爬限制。
- 低清扫描件和复杂图片 OCR 的准确性。
- 大规模资料集下的延迟、成本和题目可追溯率。
- 正式用户访谈、任务完成率与训练价值。

提交材料中的简版验收说明见 [submission-package/supporting/TEST_REPORT.md](./submission-package/supporting/TEST_REPORT.md)。
