# 数据来源策略

## 第一版入口

1. **目标自动搜索**：真实模式通过 Tavily `POST /search`，固定 `include_answer=false`，因此搜索引擎生成答案不会被当作证据。结果只是候选线索。
2. **公开 URL 导入**：服务端逐跳校验地址后读取正文；保存最终 URL、抓取时间、正文范围、内容哈希与 success/partial/failed 状态。
3. **上传或粘贴**：支持文本型 PDF、TXT、Markdown 与脱敏文字；默认私有。扫描件无法可靠解析时返回 `needs_text`。

Tavily 适配器依据官方 Search 与 Extract API：Bearer 鉴权、`basic` 深度、最多 10 个搜索结果、最多 20 个抽取 URL。当前没有密钥，因此只通过 fixture 验证请求边界，未做真实 smoke test。

## 来源类型和写法

- 官网：可表述为学校/院系公开事实，但仍绑定年份和批次。
- 学术论文/导师主页：只陈述可核实的经历、主题、贡献与公开资源。
- 亲历经验：写成“某来源作者称/回忆/评价”，不升级为学校事实。
- 转载：保留转载关系，不能增加独立经历数。
- 用户上传/粘贴：默认私有；公开导出不暴露地址和完整私密正文。
- 演示材料：只对应 `/demo-sources/*`，所有实体均为虚构。

## 公开索引的处理

CS-BAOYAN 相关仓库可作为发现线索的入口，但本版本没有复制其中文章、题目或数据，也没有把它们作为当前年份政策。软件仓库许可证不自动覆盖仓库中由第三方创作的文章、试题或转载内容。若后续接入，应逐条记录仓库 commit、文件路径、文章原作者与可追溯的官方原文。

## 演示来源

演示只使用三个内部虚构页面：招生说明结构、营员复盘结构、导师研究页结构。它们用于验证 Source → Evidence → Claim → 训练的交互与引用定位，不影射任何真实学校、导师或考生。
